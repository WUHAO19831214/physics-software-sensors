# Synthetic OCR sample frames

The PNG files and `manifest.json` are generated by `generate_samples.py`. They imitate a generic numeric screen region only; they are not Langwei/device screenshots and are not experimental measurements.
