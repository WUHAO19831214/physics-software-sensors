# Python Color Marker 最小示例

安装：

```bash
python -m pip install -e 'packages/python[color-marker]'
python examples/python-color-marker/main.py
```

示例只创建一个合成 BGR 标记帧并打印 SensorEvent，不访问摄像头、不写文件，也不代表真实实验精度。
