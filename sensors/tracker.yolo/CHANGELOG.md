# Changelog — tracker.yolo

## 0.5.0 — 2026-08-16

- Added experimental Python `YoloTrackerSensor` and public multi-target event mapping.
- Added explicit `ModelArtifact`, offline `RecordedDetectorBackend`, optional `YoloDetectorBackend`, and person-only OpenCV HOG fallback.
- Added all/ID/name class filters, ByteTrack/detection separation, native-ID metadata, centroid association and backend fallback flags.
- Added source-executed golden, offline lifecycle/fallback tests, CameraSource composition, deterministic example, assets and adapter benchmark.
- Added model/runtime/weight license review. No model weight is distributed and real inference remains not executed.

## 0.1.0 — 2026-08-04

- Contract and source inventory only; no repository implementation.
