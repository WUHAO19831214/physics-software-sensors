# Sensor Proposal

## Candidate

- Candidate name:
- Proposed Sensor ID:
- Proposed implementation language: `python` / `typescript`
- Proposed category: `capture` / `ocr` / `tracking` / `other`

## Fixed source

- Source project/repository:
- Source commit (full 40-character SHA):
- Source files:
- Source classes/functions/symbols:
- License status and evidence:

## Physics and observation boundary

- Physical experiment use:
- Directly observed (for example frame, screen pixels, OCR text, pixel centroid, bbox):
- Derived downstream quantities (for example displacement, velocity, force, amplitude, frequency, angle):
- Required calibration/units/uncertainty treatment:

## Contract and reuse

- Input:
- Output:
- Potential cross-project reuse:
- Known dependencies/artifacts:
- Existing tests, fixtures and real use evidence:
- Why this belongs in Physics Software Sensors:
- Why this should not remain project-local:

## Intake decision

- Decision: `ACCEPT` / `DEFER` / `REJECT`
- Reason:
- Reviewer/date:
- Conditions before extraction:

Do not write a placeholder commit or invent a benchmark. If the commit is unknown, resolve and pin it before deciding `ACCEPT`.
